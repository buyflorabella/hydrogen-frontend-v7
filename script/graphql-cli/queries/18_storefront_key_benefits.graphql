query TestKeyBenefitsMetafield($handle: String!) {
  productByHandle(handle: $handle) {
    id
    title
    keyBenefits: metafield(namespace: "custom", key: "key_benefits_parsable") {
      value
      type
    }
  }
}
